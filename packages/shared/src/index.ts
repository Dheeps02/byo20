/**
 * @byo20/shared — types and WS message schemas shared between renderer and server.
 * Zero runtime dependencies except Zod. The renderer may ONLY import from this package.
 */
export * from './types'
export * from './messages'
